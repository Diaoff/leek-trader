# 股票模拟交易系统设计文档

## 1\. 项目概述

本项目旨在设计并实现一个功能完整的股票模拟交易程序，为用户提供无风险的股票交易实践环境，支持自动化策略交易、行情分析与盈亏追踪。系统采用模块化、可扩展的架构设计，预留了完整的多租户 SaaS 演进能力，可支撑未来从单用户工具向企业级云服务的平滑升级。

### 核心目标

- 提供真实的模拟交易环境，还原实盘交易的流程与规则

- 支持可配置的自动化交易策略，降低用户交易决策门槛

- 提供完整的交易数据追踪与可视化分析，辅助用户提升交易能力

- 构建可扩展的底层架构，支撑未来 SaaS 化多租户服务的演进

## 2\. 整体设计思路

系统采用**前后端分离 \+ 模块化微内核**的架构设计，核心遵循以下设计原则：

1. **职责单一**：每个核心模块独立负责单一领域功能，模块间通过标准化接口通信

2. **可扩展性**：采用插件化设计，支持策略、数据源、功能模块的热插拔扩展

3. **租户透明**：底层数据访问层封装租户隔离逻辑，上层业务逻辑无需感知租户差异

4. **事件驱动**：基于事件驱动模型处理行情更新、策略信号、交易执行等异步流程

5. **容错降级**：核心链路实现多级容错与降级机制，保障系统在异常场景下的可用性

整体架构分层如下：

```Plain
┌─────────────────────────────────────────────────┐
│ 前端交互层 (Web UI)                             │
├─────────────────────────────────────────────────┤
│ 应用服务层 (API服务/租户管理/认证授权)            │
├─────────────────────────────────────────────────┤
│ 核心业务层 (行情/策略/交易/风控/分析)            │
├─────────────────────────────────────────────────┤
│ 数据访问层 (多租户数据隔离/数据源适配)           │
├─────────────────────────────────────────────────┤
│ 基础设施层 (数据库/缓存/消息队列/监控日志)      │
└─────────────────────────────────────────────────┘
```

## 3\. 技术栈选型

### 3\.1 后端技术栈

|组件|技术选型|选型说明|
|---|---|---|
|API 框架|FastAPI|高性能异步 API 框架，支持自动文档生成，适配高并发行情接口|
|异步任务|Celery \+ Redis|处理异步行情拉取、策略执行、定时任务，支持任务优先级与重试|
|ORM 框架|SQLAlchemy|支持多租户数据隔离的灵活适配，兼容多种数据库|
|数据库|PostgreSQL|支持 Schema 隔离，原生支持 JSON 字段存储配置数据，事务能力强|
|缓存|Redis|缓存实时行情数据、租户配置、限流计数，提升访问性能|
|规则引擎|Pyke \+ 自定义规则解析|实现可配置的策略规则解析，支持用户自定义交易条件|
|量化工具|TA\-Lib \+ AkShare|技术指标计算、行情数据获取，提供标准化的量化能力|
|日志框架|Loguru|结构化日志，支持多维度日志分类与持久化|

### 3\.2 前端技术栈

|组件|技术选型|选型说明|
|---|---|---|
|前端框架|Vue 3 \+ Vite \+Tailwind CSS|轻量高效的前端框架，支持按需加载，提升页面响应速度|
|UI 组件|Element Plus|成熟的企业级 UI 组件库，快速构建交互友好的管理界面|
|可视化|ECharts|高性能图表库，支持 K 线图、收益曲线、持仓分布等金融可视化|
|状态管理|Pinia|轻量的状态管理工具，管理全局用户、持仓、行情状态|
|网络请求|Axios|支持请求拦截、重试，适配异步 API 交互|

### 3\.3 运维与部署

|组件|技术选型|选型说明|
|---|---|---|
|容器化|Docker \+ Docker Compose|标准化部署包，支持单机快速部署，未来可平滑迁移到 K8s|
|监控|Prometheus \+ Grafana|系统指标监控，支持行情延迟、策略执行时间等业务指标监控|
|CI/CD|GitLab CI|自动化构建与部署，支持版本迭代与灰度发布|

## 4\. 核心功能模块设计

### 4\.1 股票池筛选模块

#### 功能描述

提供多维度的股票筛选机制，支持用户根据市场板块、行业分类、市值规模、财务指标等条件筛选符合要求的股票，构建自定义股票池。

#### 核心设计

1. **筛选维度支持**

    - 市场板块：沪市、深市、创业板、科创板、北交所

    - 行业分类：支持申万一级 / 二级行业分类，覆盖全市场行业

    - 市值规模：大盘股 \(\&gt;1000 亿\)、中盘股 \(100\-1000 亿\)、小盘股 \(\&lt;100 亿\)

    - 财务指标：市盈率、市净率、净利润增长率等基本面指标

    - 技术指标：近期涨跌幅、换手率、成交量等技术面指标

2. **数据支撑**

    - 每日同步全市场股票基础信息与财务数据，存储到基础数据库

    - 实时更新股票的技术指标数据，支持筛选条件的实时计算

    - 提供筛选结果的缓存机制，提升重复筛选的响应速度

### 4\.2 实时行情获取模块

#### 功能描述

集成多源股票行情数据源，实现实时股票价格、成交量、涨跌幅等关键数据的获取、清洗与更新，保障数据的准确性与及时性。本模块参考开源项目 `leek\-fund（韭菜盒子）` 的成熟实现，优化了个人用户场景下的数据源适配与请求效率。

#### 核心设计

1. **多数据源适配与降级（全免费方案，参考 leek\-fund 实现）**
系统采用**全免费主备数据源**架构，全部采用 leek\-fund 验证过的公开免费接口，无任何商业接口依赖，无需授权、无需 API Key 即可使用：

    |    数据源类型|    角色|    适用场景|    特性|
    |---|---|---|---|
    |    新浪财经公开接口（leek\-fund 默认股票源）|    主数据源|    股票实时行情|    免费无需授权，支持 A 股 / 港股 / 美股全市场，支持批量拉取，经过百万级用户验证，稳定性优秀|
    |    天天基金公开接口（leek\-fund 默认基金源）|    主数据源|    基金实时行情|    免费获取基金实时估值与净值数据，支持批量查询，覆盖全市场基金|
    |    东方财富公开接口（leek\-fund 备用源）|    备用数据源|    全品类行情|    免费公开接口，作为主数据源的备用，当主源异常时自动切换|
    |    AkShare 开源接口|    最终备用源|    全品类行情|    开源整合的免费公开接口，作为最后的兜底保障，覆盖全市场品类|

    当主数据源出现异常（限流、网络错误、数据超时）时，系统自动按顺序切换到备用数据源，全程无需用户干预，保障行情服务的连续性，所有数据源均为免费公开接口，无任何使用成本。

2. **批量拉取与智能刷新机制**
参考 leek\-fund 的实现，优化请求效率与资源利用率：

    - **批量拉取**：支持一次请求拉取多个标的的行情数据，通过新浪接口的 `list` 参数批量传入股票代码，或天天基金接口的 `Fcodes` 参数批量传入基金代码，大幅减少请求次数，降低被限流的概率

    - **智能刷新调度**：实现开市自动刷新、节假日自动停更的调度逻辑：

        - 交易时段（9:30\-11:30, 13:00\-15:00）：每 3 秒拉取一次行情，保障数据实时性

        - 盘前盘后时段：每 1 分钟拉取一次，降低资源消耗

        - 节假日 / 非交易日：自动停止轮询，避免无效请求

    - **请求去重**：对相同标的的重复请求进行合并，避免短时间内的重复调用

3. **限流与重试机制**

    - 客户端限流：实现**全局令牌桶 \+ 接口级滑动窗口**的双层限流模型，控制请求频率，避免触发数据源限流

    - 智能重试：采用**指数退避 \+ 抖动**的重试策略，区分临时性故障与永久性故障，对临时性错误自动重试，永久性错误直接切换数据源

    - 重试上限：连续重试 5 次失败后，自动触发数据源切换，同时触发告警通知

4. **数据清洗与缓存**

    - 针对公开接口的特殊格式进行适配：如新浪接口的 JS 格式返回、天天基金接口的 JSONP 格式返回，自动解析提取标准行情数据

    - 对获取的原始行情数据进行格式标准化、异常值过滤，保障数据一致性

    - 实时行情数据缓存到 Redis，过期时间设置为 15 秒，减少重复请求，提升访问性能

5. **个股 AI 分析（参考 leek\-fund 实现）**
参考 leek\-fund 的成熟 AI 分析能力，为个股提供智能分析功能，辅助用户快速解读个股：

    - 多维度数据聚合：针对 A 股个股自动聚合三类核心分析数据：个股历史交易与技术指标、韭研公社社区讨论、雪球热门资讯，为大模型提供全面的输入

    - 大模型智能解读：将聚合后的数据输入大模型，自动生成个股的多维分析报告，包括：当前趋势判断、消息面影响、社区情绪解读、潜在风险提示

    - 灵活模型配置：支持用户自定义大模型 API Key，默认适配阿里心流大模型（提供每日免费额度），兼容 OpenAI、DeepSeek 等主流大模型接口，用户可根据需求自由选择

    - 轻量化交互：在个股详情页提供 AI 分析入口，点击即可触发分析，无需复杂操作，快速获取 AI 对个股的专业解读

### 4\.3 可配置策略引擎模块

#### 功能描述

开发可配置的策略引擎，支持基于技术指标、基本面数据或自定义规则生成交易信号，实现策略的可视化配置与自动化执行。

#### 核心设计

1. **插件化策略架构**
系统采用插件化的策略设计，内置常用的基础策略，同时支持用户自定义策略的扩展：

    - 内置策略：双均线策略、MACD 策略、RSI 策略、布林带策略、网格策略等

    - 自定义策略：支持用户通过规则配置或脚本上传的方式，自定义交易逻辑

2. **可配置规则引擎**
提供可视化的规则配置界面，用户无需编码即可配置交易规则：

    - 条件配置：支持多条件组合，如`\(5日均线上穿20日均线\) AND \(MACD金叉\) AND \(换手率\&gt;5%\)`

    - 参数配置：支持策略参数的自定义，如均线周期、MACD 参数、止盈止损比例等

    - 信号生成：引擎根据配置的规则，实时计算行情数据，自动生成买入 / 卖出信号

3. **策略生命周期管理**

    - 策略回测：支持基于历史数据的策略回测，验证策略的有效性

    - 启停控制：支持策略的启动、暂停、停止，用户可随时控制策略的运行状态

    - 绩效监控：实时监控策略的收益率、胜率、最大回撤等绩效指标

### 4\.4 自动交易执行模块

#### 功能描述

实现模拟买入 / 卖出功能，包括订单价格确定、仓位管理、交易数量计算、风控校验等自动化流程，模拟真实的交易撮合机制。

#### 核心设计

1. **订单处理流程**

    ```Plain
    接收策略信号 → 风控规则校验 → 订单参数计算 → 模拟撮合 → 持仓更新 → 交易记录保存
    ```

2. **自动化参数计算**

    - 订单价格：默认使用当前最新行情价格，支持用户配置限价单价格

    - 仓位管理：支持用户配置单笔交易的仓位比例（如总资金的 10%），自动计算可交易数量

    - 数量计算：根据订单价格、仓位比例，计算可买入的股票数量（取整为 100 的整数倍，符合 A 股交易规则）

3. **模拟撮合机制**
模拟 A 股的 T\+1 交易规则，以及涨跌停限制：

    - 涨跌停判断：当股票价格达到涨跌停板时，拒绝对应的交易订单

    - 停牌处理：对停牌股票，禁止所有交易操作

    - 撮合延迟：模拟实盘的订单撮合延迟，支持异步订单处理

### 4\.5 盈亏记录与分析模块

#### 功能描述

建立完整的交易记录系统，实时追踪持仓情况、计算浮动盈亏与实际盈亏，并提供可视化的盈亏分析报表。

#### 核心设计

1. **交易记录系统**

    - 完整记录每笔交易的：交易时间、股票代码、交易类型（买入 / 卖出）、成交价格、成交数量、手续费、盈亏金额

    - 持仓记录：实时更新每只股票的持仓数量、成本价、当前价、浮动盈亏、持仓比例

    - 账户流水：记录账户的资金变动，包括入金、出金、交易资金变动、盈亏结算

2. **盈亏计算**

    - 浮动盈亏：根据当前持仓数量、成本价与最新行情价格，实时计算未平仓的盈亏

    - 实际盈亏：对平仓的交易，计算实际的盈亏金额与收益率

    - 累计收益：计算账户的累计收益率、年化收益率、最大回撤等核心指标

3. **可视化分析报表**

    - 资产曲线：展示账户资产的历史变化曲线，对比大盘基准收益

    - 持仓分布：展示当前持仓的行业分布、市值分布、仓位占比

    - 交易统计：展示月度 / 年度的交易次数、胜率、盈亏比、平均持仓时间

    - 策略绩效：展示每个策略的历史绩效，对比不同策略的收益表现

### 4\.6 收盘自动复盘（参考 leek\-fund 实现）

#### 功能描述

参考 leek\-fund 的 AI 资讯汇总能力，实现收盘后的自动复盘功能，帮助用户快速总结当日市场与个人交易情况，将原本需要半小时的人工复盘压缩到分钟级，大幅提升复盘效率。

#### 核心设计

1. **自动触发调度**：每日 A 股收盘后（15:05）自动触发复盘任务，非交易日自动跳过，无需用户手动操作

2. **多维度复盘数据聚合**：

    - 市场维度：自动汇总当日的市场资讯、选股宝快讯、板块涨跌数据、北向 / 南向资金流向

    - 个人维度：自动汇总用户当日的交易记录、持仓表现、策略运行结果、账户盈亏情况

3. **AI 结构化复盘报告**：
将聚合后的全维度数据输入大模型，自动生成结构化的复盘报告，包含三大核心部分：

    - 市场脉络分析：当日市场的核心驱动因素、热点板块梳理、全局风险提示

    - 个人交易复盘：用户当日交易的行为分析、持仓与板块的表现对比、操作的优化建议

    - 次日关注线索：整理次日需要重点关注的事件、潜在的标的机会与风险点

4. **报告分发与存储**：
自动将复盘报告推送到用户的消息中心，支持邮件、站内信订阅推送，同时永久存储所有历史复盘报告，用户可随时回溯查看历史复盘记录

## 5\. 多租户 SaaS 演进计划

系统在设计阶段已完整预留多租户能力，支持从单用户系统向 SaaS 平台的平滑演进，无需重构核心业务代码。

### 5\.1 多租户数据隔离方案

系统采用**演进式数据隔离**策略，根据租户规模与需求，支持三种隔离模式的无缝切换：

|隔离模式|适用阶段|隔离级别|优势|成本|
|---|---|---|---|---|
|共享库共享表（Shared Database, Shared Schema）|初期（租户 \&lt; 1000）|逻辑隔离|运维简单，资源利用率高，成本最低|低|
|共享库独立 Schema（Shared Database, Separate Schema）|中期（1000 \&lt; 租户 \&lt; 10000）|逻辑 \- 物理混合隔离|数据隔离性好，支持租户自定义配置，运维成本适中|中|
|独立数据库（Separate Database）|后期（大型企业租户）|物理隔离|最高级别的数据隔离，满足合规要求，支持租户自定义架构|高|

#### 实现细节

1. **预留租户标识**：所有业务表均预留`tenant\_id`字段，在共享表模式下，通过该字段区分不同租户的数据

2. **透明化数据访问**：数据访问层封装租户隔离逻辑，对上层业务逻辑完全透明：

    - 共享表模式：自动在查询条件中添加`tenant\_id = 当前租户`的过滤条件

    - 独立 Schema 模式：自动切换数据库的 Search Path 到租户对应的 Schema

    - 独立数据库模式：自动切换数据库连接到租户专属的数据库实例

3. **平滑迁移**：支持租户在不同隔离模式之间的平滑迁移，无需停机，对用户完全透明

### 5\.2 租户管理体系

1. **租户生命周期管理**：支持租户的注册、试用、付费、升级、注销全生命周期管理

2. **资源配额控制**：对每个租户配置资源配额，防止资源滥用：

    - API 调用配额：限制租户的行情 API 调用频率

    - 策略数量配额：限制租户可创建的策略数量

    - 交易次数配额：限制租户的每日交易次数

    - 存储配额：限制租户的历史数据存储容量

3. **租户个性化配置**：支持租户自定义风控规则、数据源选择、界面主题等个性化配置

4. **权限管理**：支持租户内的多用户权限管理，不同用户拥有不同的操作权限，如管理员、交易员、观察者等

### 5\.3 SaaS 化演进路线

|阶段|时间规划|核心能力|租户规模|
|---|---|---|---|
|单用户版|第一阶段|单用户模拟交易，本地部署|个人用户|
|多租户基础版|第二阶段|共享库共享表模式，基础 SaaS 能力|中小租户，\&lt;1000|
|多租户企业版|第三阶段|支持独立 Schema / 独立数据库，企业级功能|全量租户，\&lt;10000|
|云原生平台|第四阶段|容器化编排，弹性扩缩容，全球化部署|大规模租户，\&gt;10000|

## 6\. 风险控制机制

系统实现了严格的风险控制机制，保障用户的模拟交易风险可控，避免过度交易与非理性投资。

### 6\.1 核心风控规则

1. **单日最大交易次数限制**：用户可配置单日最大交易次数，超过后拒绝新的交易订单，避免过度交易

2. **单只股票最大持仓比例**：限制单只股票的持仓占总资金的比例（默认 20%），避免单一标的风险过度集中

3. **总仓位上限**：限制用户的总持仓比例，默认不超过 90%，预留部分资金应对风险

4. **每日亏损熔断**：当账户当日亏损达到设定阈值（默认 5%）时，自动停止所有新的开仓操作，强制止损

5. **标的过滤**：自动过滤 ST 股、退市预警股、停牌股，禁止交易高风险标的

6. **交易时间校验**：仅在 A 股交易时段（9:30\-11:30, 13:00\-15:00）允许交易，非交易时段拒绝订单

### 6\.2 风控执行流程

所有交易订单在执行前，都会经过完整的风控规则校验，只有所有规则校验通过后，才会执行交易：

```Plain
订单提交 → 交易时间校验 → 标的风险校验 → 交易次数校验 → 持仓比例校验 → 亏损熔断校验 → 执行交易
```

任意一个校验不通过，都会拒绝订单，并返回详细的拒绝原因，记录到系统日志中。

## 7\. 异常处理与可靠性保障

### 7\.1 网络异常处理

- 断线重连：对 WebSocket 行情连接，实现自动断线重连机制，重连后自动恢复订阅

- 请求重试：对网络超时、连接错误等临时性错误，自动进行指数退避重试

- 降级处理：当所有数据源都不可用时，系统自动切换到降级模式，使用缓存的最新行情数据，保障核心功能可用

### 7\.2 数据延迟处理

- 数据时间戳校验：对获取的行情数据，校验数据的时间戳，当数据延迟超过 30 秒时，标记为过期数据，提醒用户

- 延迟监控：实时监控行情数据的延迟，当延迟超过阈值时，触发告警，自动切换数据源

- 数据补全：对缺失的行情数据，自动进行补全，保障 K 线数据的完整性

### 7\.3 系统容错

- 任务幂等：所有异步任务实现幂等性，避免重复执行导致的错误

- 事务保障：核心交易流程使用数据库事务，保障交易数据的一致性

- 备份恢复：定期备份数据库数据，支持数据的快速恢复，避免数据丢失

## 8\. 日志与监控系统

### 8\.1 日志系统

系统实现了完整的日志记录功能，记录所有交易决策、执行过程和系统运行状态，支持审计与问题排查：

1. **交易日志**：记录所有交易相关的操作，包括：

    - 策略信号生成日志：记录信号生成的时间、股票、策略、触发条件

    - 订单执行日志：记录订单的提交、校验、执行、结果的完整流程

    - 资金变动日志：记录账户资金的所有变动记录

2. **系统日志**：记录系统的运行状态，包括：

    - 行情拉取日志：记录每次行情拉取的数据源、时间、结果、耗时

    - 策略运行日志：记录策略的运行时间、执行结果、异常信息

    - 系统错误日志：记录系统的异常、错误信息，用于问题排查

3. **审计日志**：记录用户的所有操作，包括登录、配置修改、策略启停等，支持安全审计

所有日志均采用结构化存储，支持按租户、时间、类型等多维度的查询与筛选，日志数据永久保存，满足审计要求。

### 8\.2 监控系统

系统实现了全维度的监控，保障系统的稳定运行：

1. **基础设施监控**：监控服务器的 CPU、内存、磁盘、网络使用率

2. **应用监控**：监控 API 的响应时间、错误率、请求量

3. **业务监控**：

    - 行情延迟监控：监控行情数据的延迟情况

    - 策略执行监控：监控策略的执行时间、成功率

    - 交易成功率监控：监控交易订单的成功率

4. **告警机制**：当监控指标超过阈值时，自动触发邮件、短信告警，通知运维人员及时处理

## 9\. 用户交互界面设计

系统设计了用户友好的交互界面，将核心功能整合为统一的工作台，主要包含以下页面：

### 9\.1 仪表盘页面

展示账户的核心概览信息：

- 账户总资金、可用资金、持仓市值

- 当日收益、累计收益、浮动盈亏

- 待执行策略、今日交易记录

- 资产变化曲线、核心绩效指标

### 9\.2 行情与选股页面

- 实时行情列表：展示自选股的实时价格、涨跌幅、成交量

- K 线图：展示股票的历史 K 线、技术指标

- 股票筛选器：提供多维度的股票筛选条件，构建自定义股票池

### 9\.3 策略管理页面

- 策略列表：展示用户创建的所有策略，支持启停、编辑、删除

- 策略配置：可视化的策略规则配置界面，支持条件拖拽、参数配置

- 策略回测：展示策略的回测结果，包括收益曲线、绩效指标

### 9\.4 交易与持仓页面

- 持仓列表：展示当前持仓的股票、成本价、当前价、浮动盈亏

- 交易记录：展示历史交易记录，支持筛选与导出

- 订单管理：展示当前的订单状态，支持订单撤销

### 9\.5 盈亏分析页面

- 资产曲线：展示账户的历史资产变化，对比大盘收益

- 持仓分布：展示持仓的行业、市值分布

- 交易统计：展示月度收益、交易胜率、盈亏比等统计数据

- 策略绩效：展示不同策略的绩效对比

## 10\. 系统扩展性设计

系统采用松耦合的模块化设计，保障系统的可扩展性，便于后续功能升级和策略优化：

1. **插件化扩展**：核心模块均采用插件化设计，支持：

    - 策略插件：新增交易策略无需修改核心代码，开发插件即可接入

    - 数据源插件：新增行情数据源只需开发适配插件，即可接入系统

    - 功能插件：支持新增功能模块，如期货模拟、期权模拟等

2. **接口标准化**：所有模块间的接口均采用标准化设计，支持模块的独立升级与替换

3. **配置化驱动**：核心规则、参数均支持配置化，无需修改代码即可调整系统行为

4. **API 开放**：提供开放的 REST API，支持第三方系统的对接与集成



> （注：文档部分内容可能由 AI 生成）
